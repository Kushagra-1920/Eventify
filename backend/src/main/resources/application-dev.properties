# Dev Environment (Local)

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/ticket_booking?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Local Defaults for Secrets (Safe to commit for local testing ONLY)
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
razorpay.key.id=rzp_test_SUNvosgFmwxxZ3
razorpay.key.secret=4Of8hLJp7WL9WLhjIL1RUn1v

# OAuth
# NOTE: OAuth requires real credentials even locally, which can be injected via .env during dev
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:local_placeholder_client_id}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:local_placeholder_secret}

# CORS & Frontend URL
app.frontend.url=http://localhost:5173
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000
